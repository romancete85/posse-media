# Secretos — NUNCA versionar
.env
*.env
tokens.json
*.pem

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
build/
dist/
.pytest_cache/
.ruff_cache/

# OS / editor
.DS_Store
.idea/
.vscode/

# CV/PDF con PII — no versionar (el pipeline solo lee context/*.md)
context/*.pdf

# Imagenes de referencia de terceros (watermark) — no versionar
context/*.png
